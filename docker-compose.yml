services:
  google-sheets-mcp-community:
    build: .
    container_name: google-sheets-mcp-community
    restart: unless-stopped
    ports:
      - "127.0.0.1:3032:3000"
    mem_limit: 512m
    mem_reservation: 256m
